
const Title = ({firstTitle,secondTitle}) => {
    return (
        <div>
            <h1 className="text-4xl font-bold website-title mb-2">{firstTitle}</h1>
            <h4 className="">{secondTitle}</h4>
        </div>
    );
};

export default Title;